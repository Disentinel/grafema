/**
 * MCP Knowledge Handler Tests (REG-629)
 *
 * Knowledge handlers disabled (US-17) — entire test file skipped.
 * Re-enable when git-ingest is implemented.
 */

import { describe, it } from 'node:test';

describe('KnowledgeHandlers (REG-629)', { skip: 'knowledge handlers disabled (US-17)' }, () => {
  it('placeholder', () => {});
});
