# Steve Jobs Demo — REG-378

## Demo Script (Expected)
1. In ToolJet repo:
   - `npx @grafema/cli init`
   - `npx @grafema/cli analyze --auto-start`
2. Observe progress output (node/edge counts updating).
3. Verify command exits to shell after analysis completes (no manual interrupt).

## Result
- Demo not run here (Node.js not available in environment).
- Must be validated on a Node-enabled machine.
