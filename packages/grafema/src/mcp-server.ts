#!/usr/bin/env node
/**
 * Grafema MCP Server entry point (unified package)
 *
 * Re-exports from @grafema/mcp. This file exists so that
 * `npm install grafema` provides the `grafema-mcp` command.
 */

import '@grafema/mcp';
