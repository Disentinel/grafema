#!/usr/bin/env node
/**
 * Grafema CLI entry point (unified package)
 *
 * Re-exports from @grafema/cli. This file exists so that
 * `npm install grafema` provides the `grafema` command.
 */

// Just import and run the CLI
import '@grafema/cli';
