---
id: kb:session:2026-04-17-reg-1100-hex-atlas-2d-3d
type: SESSION
task_id: REG-1100
projections:
  - epistemic
created: 2026-04-17
---

## REG-1100: unified HexAtlas 2D/3D + rfdb UI + VS Code retarget

**Scope:** single React component `<HexAtlas mode>` with runtime 2D⇄3D switch, served by rfdb-server at `/ui/{db}` (Cargo feature `ui`, default on, rust-embed), iframed by VS Code webview. Archived standalone `packages/gui-server` binary.

**Workflow:** v3 — 3 Dijkstra verification passes (2 REJECT → revise) + 9 implementation phases + 3-review (Steve / Вадим auto / Uncle Bob, 2 REJECT→fix rounds to converge).

**Dates:** 2026-04-17 → 2026-04-18.

**Tests landed:** 237 GUI unit + 171 VS Code unit + 20 rfdb-server integration (compile_without_ui, static_ui, ui_routes). All green at merge.

**Commits:**
- 20a50eb1 — `docs(REG-1100): session artifacts — plan + Dijkstra reviews + impl summary`
- 154c059c — `feat(REG-1100): unified HexAtlas 2D/3D + rfdb UI + VS Code retarget`

**Deferred DAIs** (tracked post-ship): rfdb-server HTTP port hardcoded 3335, db hardcoded 'default', openMap doesn't auto-start rfdb.

**Behavioral changes (user-acknowledged):** click-on-selected-tile now idempotent replace (was toggle-off); tooltip shows file:line row when node.line defined.

**Skills extracted:**
- `tsx-jsx-runtime-mismatch-vite-build-vs-test` (user-global)

**Key files:** HexAtlas.tsx, controller/SceneApi.ts, three/SceneManager.ts (setMode), three/HullLayer.ts (re-enabled), geom/hull.ts (exact-boundary port), hooks/canvasBuild.ts (Canvas.tsx 836→206 split), packages/rfdb-server/src/static_ui.rs + http_server.rs UI routes, packages/vscode/src/mapPanel.ts retarget.
