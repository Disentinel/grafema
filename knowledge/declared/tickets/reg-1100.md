---
id: kb:ticket:reg-1100
type: TICKET
projections:
  - epistemic
created: 2026-04-17
---

## REG-1100: hex-atlas — extract layout from view, abstract controller for 2D/3D

**Linear:** https://linear.app/grafemadev/issue/REG-1100
**Team:** Reginaflow | **Project:** Grafema | **Status:** In Review (awaiting merge) | **Labels:** v0.3, Improvement
**Blocks:** REG-1101 (@grafema/hex-atlas standalone npm package)
**Related:** REG-1102 (Rust hex-atlas greedy pack port)

### Scope (as updated 2026-04-17)
1. Layout data contract — `fetchLayout(source, signal)` wraps fixture + stream.
2. Shared controller — SceneApi imperative handle + 16-method interface.
3. Single `<HexAtlas mode>` component with runtime 2D⇄3D switch.
4. RFDB HTTP UI — rfdb-server serves `/ui/{db}` via Cargo feature `ui` + rust-embed.
5. VS Code webview — iframes rfdb's /ui/{db}, drops grafema-gui binary.
6. Feature-gap audit — unify 2D sandbox + 3D GUI surfaces.

### Produced in session
kb:session:2026-04-17-reg-1100-hex-atlas-2d-3d
