# VS Code Autoresearch Questions
# Sourced from REAL GitHub issues where people struggled to understand VS Code internals
# Target: grafema graph on microsoft/vscode (~3-4M nodes, ~800K resolution edges)
#
# Selection criteria:
# - Real person asked a real question (not synthetic)
# - Required non-trivial code navigation to answer
# - Spans multiple files/services
# - Answer is verifiable from source code

# === From GitHub Issues — People struggling with architecture ===

- id: Q01
  question: "Which of IHoverService, IHoverDelegate, or setupCustomHover should I use to add a custom hover to a new component? Why is setupCustomHover associated with IconLabel?"
  source: "https://github.com/microsoft/vscode/issues/204965"
  type: api_confusion
  sillito_level: 2
  why: "Real VS Code contributor confused by 3 overlapping hover APIs. 8 comments, no clear resolution. Requires tracing all callers of each API to understand the intended pattern."
  golden_answer_hint: "IHoverDelegate for base widgets, IHoverService directly for simple cases, setupCustomHover is legacy from pre-IHoverService era"

- id: Q02
  question: "In ExplorerView#getActiveFile, why does it only use file-URIs? Why aren't remote files revealed in the explorer?"
  source: "https://github.com/microsoft/vscode/issues/34768"
  type: root_cause
  sillito_level: 3
  why: "Contributor traced code to the exact line (explorerView.ts#L363) and couldn't understand the design decision. Requires understanding URI scheme handling across the explorer."
  golden_answer_hint: "explorerView.ts line 363, file scheme check, related to IExplorerService model"

- id: Q03
  question: "When the first workspace folder changes, should the extension host be restarted? What happens to workspace.rootPath during runtime?"
  source: "https://github.com/microsoft/vscode/issues/69335"
  type: architectural_investigation
  sillito_level: 4
  why: "64 comments. Core team investigation into extension host lifecycle. Understanding requires tracing workspace.rootPath, onDidChangeWorkspaceFolders, and extension host restart logic."
  golden_answer_hint: "ExtensionHostManager restart decision, workspace.rootPath immutability contract, IExtHostWorkspace"

- id: Q04
  question: "Why does the Extension Host terminate unexpectedly in large repos? What triggers the crash and what is the recovery path?"
  source: "https://github.com/microsoft/vscode/issues/23976"
  type: debugging
  sillito_level: 3
  why: "69 comments, recurring issue. Requires understanding extension host process lifecycle, memory limits, and the restart mechanism."
  golden_answer_hint: "ExtensionHostProcessWorker, OOM in extension host process, IExtensionHostStarter recovery"

- id: Q05
  question: "How does VS Code's terminal pty host connection work? What causes 'Connection to terminal pty host process is becoming unresponsive'?"
  source: "https://github.com/microsoft/vscode/issues/130320"
  type: root_cause
  sillito_level: 3
  why: "87 comments, started after 1.59 update. Root cause requires tracing TerminalInstance → PtyHostService → pty process communication and understanding the heartbeat/reconnection logic."
  golden_answer_hint: "PtyHostService, TerminalProcessManager, heartbeat mechanism, reconnection logic"

- id: Q06
  question: "When settings editor runs concurrent AI search and local search, why do the timing telemetry values get corrupted?"
  source: "https://github.com/microsoft/vscode/pull/304361"
  type: root_cause
  sillito_level: 3
  why: "Real bug. Shared class-level StopWatch instance. PR explains root cause: createCancelablePromise launches concurrent searches, shared stopWatch.reset() corrupts timing. Requires tracing SettingsEditor search flow."
  golden_answer_hint: "SettingsEditor2, shared StopWatch, createCancelablePromise, fix: local StopWatch per search"

- id: Q07
  question: "Why does the Watch view context menu fail to appear during debugging when the Variables pane is hidden?"
  source: "https://github.com/microsoft/vscode/pull/274414"
  type: root_cause
  sillito_level: 3
  why: "Real bug. Context menu visibility tied to Variables pane state. Requires understanding debug view contribution points and context key dependencies."
  golden_answer_hint: "WatchExpressionsView, context menu precondition, Variables pane visibility context key"

- id: Q08
  question: "In notebooks, when using Cmd+D (Add Selection to Next Find Match), what prevents infinite looping when all matches are already selected?"
  source: "https://github.com/microsoft/vscode/pull/251157"
  type: root_cause
  sillito_level: 3
  why: "Real bug — infinite loop. Requires understanding NotebookFindController state machine, selection tracking, and loop termination condition."
  golden_answer_hint: "NotebookFindController, addFindMatchToSelection, set-based dedup of already-selected matches"

# === From Contributing Difficulty — New contributors struggling ===

- id: Q09
  question: "I want to contribute to VS Code but I don't understand how the codebase is structured. What are the main layers and how do services connect?"
  source: "https://github.com/microsoft/vscode/issues/87492"
  type: onboarding
  sillito_level: 4
  why: "6 comments from someone genuinely trying to contribute. The answer requires mapping VS Code's layered architecture: base → platform → editor → workbench → extensions."
  golden_answer_hint: "src/vs/base (utilities), src/vs/platform (services), src/vs/editor (monaco), src/vs/workbench (shell), dependency injection via InstantiationService"

- id: Q10
  question: "What docs page for extension development is hardest to understand? What topics lack useful information?"
  source: "https://github.com/microsoft/vscode/issues/58226"
  type: documentation_gaps
  sillito_level: 1
  why: "35 comments collecting real pain points. Each comment is a question someone couldn't answer from existing docs. Gold mine for what Grafema should help with."
  golden_answer_hint: "Top complaints: webview lifecycle, tree view data providers, custom editors, debugging extension host"

# === Architecture Questions — from PR descriptions & code comments ===

- id: Q11
  question: "How does VS Code's DI system work? When I see @IConfigurationService in a constructor, how does the value get injected?"
  source: "deduced from codebase patterns + issue #204965 mentioning service confusion"
  type: architectural
  sillito_level: 2
  why: "Every VS Code file uses DI. Understanding createDecorator → ServiceIdentifier → InstantiationService.createInstance is prerequisite for any contribution."
  golden_answer_hint: "createDecorator in instantiation.ts, ServiceIdentifier, InstantiationService._createInstance, registerSingleton in workbench contributions"

- id: Q12
  question: "How does the extension host protocol serialize API calls? When an extension calls vscode.workspace.getConfiguration, what happens across the process boundary?"
  source: "deduced from issues #113410 #114714 #23976 (extension host crashes)"
  type: dataflow
  sillito_level: 3
  why: "Extension host crashes are the #1 stability issue. Understanding RPCProtocol, ProxyIdentifier, and the main↔exthost serialization is essential for debugging."
  golden_answer_hint: "RPCProtocol in vs/workbench/services/extensions, ProxyIdentifier, MainThreadExtensionService, ExtHostExtensionService"

# === From PR Root Cause Patterns — Structural understanding needed ===

- id: Q13
  question: "The git extension prompts to configure identity when user.name/email is missing. How does the git extension detect this failure and what recovery flow does it offer?"
  source: "https://github.com/microsoft/vscode/pull/304522"
  type: feature_trace
  sillito_level: 2
  why: "Recent PR. Tracing from git commit failure → error detection → inline prompt → retry. Tests understanding of extensions/git/src/commands.ts flow."
  golden_answer_hint: "extensions/git/src/commands.ts, commit command, git error parsing, QuickInput for name/email"

- id: Q14
  question: "Sessions feature uses ITaskService to run tasks instead of terminal commands. How does ITaskService.run() differ from direct terminal execution?"
  source: "https://github.com/microsoft/vscode/pull/304397"
  type: architecture
  sillito_level: 2
  why: "PR replaced terminal-based task execution with proper service. Understanding requires comparing terminal.sendText vs ITaskService.run lifecycle."
  golden_answer_hint: "ITaskService in vs/workbench/contrib/tasks, TaskExecution, terminal creation vs task runner"

- id: Q15
  question: "How does files.exclude actually work? What component reads the setting and how does it filter the explorer tree?"
  source: "https://github.com/microsoft/vscode/issues/120082"
  type: configuration_trace
  sillito_level: 2
  why: "8 comments, users confused by behavior. Requires tracing from IConfigurationService → file watcher → explorer model filtering."
  golden_answer_hint: "IFileService, ExplorerModel, glob matching in vs/base/common/glob.ts, IConfigurationService change events"

# === Hard Questions — Multi-step Navigation Required ===

- id: Q16
  question: "What is the complete lifecycle of a terminal instance — from 'New Terminal' click to shell process running to output appearing in the panel?"
  source: "synthesized from issues #130320, #105446, #69665, #43169 (all terminal lifecycle bugs)"
  type: lifecycle
  sillito_level: 4
  why: "Terminal bugs account for 5 of top 30 VS Code issues by comment count. Full lifecycle trace: command → TerminalService → TerminalInstance → PtyHostService → pty process → output rendering."
  golden_answer_hint: "TerminalService.createTerminal → TerminalInstance → TerminalProcessManager → PtyHostService → node-pty → xterm.js"

- id: Q17
  question: "What causes VS Code to become unresponsive after resuming from sleep? What rendering/GPU recovery path exists?"
  source: "https://github.com/microsoft/vscode/issues/69665 (145 comments), #284162 (56 comments)"
  type: root_cause
  sillito_level: 3
  why: "Two of the most-commented bugs. Requires understanding Electron's GPU process recovery, compositor state, and VS Code's redraw logic."
  golden_answer_hint: "Electron gpu-process crash recovery, IPartService.onPartVisibilityChange, terminal GPU texture invalidation"

- id: Q18
  question: "Extension host crashed with SIGILL on macOS ARM64. What is the extension host startup sequence and what could cause an illegal instruction?"
  source: "https://github.com/microsoft/vscode/issues/113410"
  type: debugging
  sillito_level: 3
  why: "89 comments, Apple Silicon transition. Requires understanding extension host process spawning, native module loading, and architecture-specific binary selection."
  golden_answer_hint: "ExtensionHostStarter, fork/exec path, native node modules, Rosetta 2 vs native ARM64"

- id: Q19
  question: "How does SSH agent forwarding work with VS Code Remote SSH? Why does SSH_AUTH_SOCK become stale?"
  source: "https://github.com/microsoft/vscode/issues/168202"
  type: root_cause
  sillito_level: 3
  why: "107 comments, ongoing issue. Requires understanding how Remote SSH extension manages environment variables, terminal persistence, and SSH socket lifecycle."
  golden_answer_hint: "Remote SSH extension, SSH_AUTH_SOCK in terminal environment, terminal persistence across reconnections"

- id: Q20
  question: "Why does CPU go 100% when using Go To Definition on local node_modules? What is the JS/TS language server doing?"
  source: "https://github.com/microsoft/vscode/issues/71866"
  type: performance
  sillito_level: 3
  why: "9 comments. Requires understanding TypeScript language service integration, project inference from node_modules, and the tsserver process lifecycle."
  golden_answer_hint: "TypeScriptServiceClient, tsserver process, automatic project inference, maxNodeModuleJsDepth setting"
